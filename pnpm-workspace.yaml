# pnpm 11 blocks package lifecycle scripts unless listed here. Each of these ships a
# platform-specific binary that its install script puts in place: esbuild's compiler, sharp's image
# codecs, and workerd, the Cloudflare Workers runtime used by `wrangler dev`.
allowBuilds:
  esbuild: true
  sharp: true
  workerd: true
